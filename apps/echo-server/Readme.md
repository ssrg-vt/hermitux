This is a simple echo server. To test it setup the tap interface with the script
if needed, and edit the makefile with network parameters if needed. THen use
`make test` to run the unikernel. You can test it with telnet:

```
telnet 10.0.5.2 8000
```
